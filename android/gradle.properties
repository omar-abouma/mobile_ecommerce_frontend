org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# If you see errors like "Type T not present" or task creation failures when running Gradle
# (often caused by running Gradle with a too-new JDK, e.g. JDK 18+ or 24), point Gradle
# at a Java 11 (or Java 17) installation. Install a JDK 11 (Temurin/Adoptium or Oracle)
# and then either set the system JAVA_HOME to that JDK or uncomment and set the
# org.gradle.java.home property below to the JDK path.
# Example (Windows path must escape the colon and backslashes):
# org.gradle.java.home=C\:\\Program Files\\Eclipse Adoptium\\jdk-11.0.20.9-hotspot
org.gradle.java.home=C\:\\Program Files\\Eclipse Adoptium\\jdk-11.0.28.6-hotspot
